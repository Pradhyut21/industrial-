fastapi>=0.115.0
uvicorn>=0.52.4
python-multipart>=0.0.9
jinja2>=3.1.4
python-dotenv
spacy
rapidfuzz
sentence-transformers
faiss-cpu
numpy
rank-bm25
httpx
pytest
faster-whisper
sqlalchemy

pytesseract
pdf2image
opencv-python-headless
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# Production-mode optional deps (safe to install; unused when env vars are unset)
redis
celery

# Continuity Intelligence Platform — document ingestion
python-pptx        # .pptx slide parsing (Continuity Vault)
python-docx        # .docx document parsing (Continuity Vault)
openpyxl           # .xlsx spreadsheet parsing (Continuity Vault)

# x402 / Algorand — AI Agent Micropayment Gate (Section 9)
# x402-avm implements the x402 HTTP 402 protocol on Algorand (GoPlausible facilitator).
# py-algorand-sdk (algosdk) is used for the verifier payout mechanic (Section 9.6).
x402-avm[fastapi,avm]==2.0.2
py-algorand-sdk

